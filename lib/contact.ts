/**
 * Where people write to us.
 *
 * One constant rather than an address typed into nine files, because the last
 * change had to touch a privacy policy, a terms page, a community guidelines
 * page, the legal layout and the campus form — and missing one of those leaves
 * a stale address in front of exactly the reader who matters.
 *
 * Used only inside mailto: links, never printed on screen, so it can be swapped
 * for hello@looprides.dev once that mailbox exists and can send as itself.
 */
export const CONTACT_EMAIL = "ac44@williams.edu";
